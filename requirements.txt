# ── Force CPU-only torch BEFORE everything else ──────────
# This prevents sentence-transformers from pulling GPU torch
# which brings 2GB+ of NVIDIA CUDA packages
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# ── LLM ──────────────────────────────────────────────────
groq
langchain
langchain-community
langchain-groq
langchain-core
langchain-text-splitters
langchain-chroma
langchain-huggingface

# ── Embeddings (local, CPU only) ─────────────────────────
sentence-transformers

# ── Vector database ───────────────────────────────────────
chromadb

# ── PDF extraction ────────────────────────────────────────
pymupdf
pytesseract
Pillow
pdfplumber

# ── API and UI ────────────────────────────────────────────
fastapi
uvicorn
streamlit
python-multipart

# ── Evaluation ───────────────────────────────────────────
ragas
datasets

# ── Testing ───────────────────────────────────────────────
pytest
pytest-asyncio
httpx

# ── Utilities ─────────────────────────────────────────────
python-dotenv
pydantic
openai